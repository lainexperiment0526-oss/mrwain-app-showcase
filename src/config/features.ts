// Centralized feature flags.
// Toggle these to enable/disable platform features without ripping out code.

/**
 * Affiliate / "Invite & Earn" program.
 * Temporarily disabled for Mainnet review. Set to `true` to fully restore
 * the affiliate UI (navigation entry, dashboard, referral capture, etc.).
 */
export const AFFILIATE_ENABLED = false;
